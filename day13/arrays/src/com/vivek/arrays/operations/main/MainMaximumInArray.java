package com.vivek.arrays.operations.main;

import com.vivek.arrays.operations.ArrayOperation;

public class MainMaximumInArray {

	public static void main(String[] args) {
		ArrayOperation array = new ArrayOperation();
		int[] arr = { 15, 30, 10, 5, 100 };
		array.findMaximum(arr);
		array.findMinimum(arr);
	}

}
